spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clientes?useSSL=false&allowMultiQueries=true&serverTimezone=UTC
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  jackson:
    serialization:
      fail-on-empty-beans: false


server:
  servlet:
    context-path: /v1
  port: 9090
